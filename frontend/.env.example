# .env.example
# For local dev, Vite proxy takes care of `/api`.
# For production (Vercel, Netlify), uncomment the line below and set to your hosted backend URL.
# VITE_API_BASE_URL=https://your-production-backend.up.railway.app/api
