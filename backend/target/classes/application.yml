spring:
  application:
    name: JusticeLinker

  # MySQL Database
  datasource:
    url: jdbc:mysql://localhost:3306/justicelinker?useSSL=false&serverTimezone=Asia/Kolkata&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: Krishna@31540

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  # Gmail SMTP
  mail:
    host: smtp.gmail.com
    port: 587
    username: justicelinker.noreply@gmail.com
    password: xsorwsmlkhpntngp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 15MB

# JWT Config
jwt:
  secret: ${JWT_SECRET:JusticeLinkerSecretKeyForJWTTokenGeneration2026MustBeAtLeast256Bits}
  expiration: 86400000  # 24 hours

# Cloudinary
cloudinary:
  cloud-name: dqccynkls
  api-key: "693998595316399"
  api-secret: o9OB1j3_vo44AW-NUkmWtIUzFsU

# App Config
app:
  max-complaints-per-month: 5
  max-images-per-complaint: 3
  max-image-size-mb: 5
  otp-expiry-minutes: 5
  frontend-url: ${FRONTEND_URL:http://localhost:5173}
  noreply-email: justicelinker.noreply@gmail.com
  support-email: justicelinker.official@gmail.com

server:
  port: 8081
